using UnityEngine;
using System;

public class StateManager : MonoBehaviour
{
    /* // Events for state changes
    public static event Action<string> OnSceneStateChanged;
    public static event Action<string> OnCameraStateChanged;

    private string currentSceneState = "Idle";
    private string currentCameraState = "Idle";


    public void ChangeSceneState(string newState)
    {
        if (currentSceneState != newState)
        {
            Debug.Log($"Scene state changed from {currentSceneState} to {newState}");
            currentSceneState = newState;
            OnSceneStateChanged?.Invoke(newState);
        }
    }


    public void ChangeCameraState(string newState)
    {
        if (currentCameraState != newState)
        {
            Debug.Log($"Camera state changed from {currentCameraState} to {newState}"); // Corrected log
            currentCameraState = newState;
            OnCameraStateChanged?.Invoke(newState);
        }
    } */

}
